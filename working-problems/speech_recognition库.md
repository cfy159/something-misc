# speech_recognition语音识别

speech_recognition库是一个用于语音识别的python包，它可以使python程序能够识别来自麦克风、音频或网络流的语音

1. 安装speech_recognition库
	pip install SpeechRecognition
2. 安装依赖pyaudio,如果需要麦克风输入，就需要安装这个包(linux系统上不能直接安装)
	pip install pyaudio
	linux: 使用包管理器下载 sudo apt install python3-pyaudio